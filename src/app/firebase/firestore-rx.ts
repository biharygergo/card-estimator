export { docData, collectionData, sortedChanges } from 'rxfire/firestore';
export {
  doc as docSnapshots,
  collection as collectionSnapshots,
} from 'rxfire/firestore';
export { user as authUser } from 'rxfire/auth';

import { ResizeMonitorDirective } from './resize-monitor.directive';

describe('ResizeMonitorDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeMonitorDirective();
    expect(directive).toBeTruthy();
  });
});

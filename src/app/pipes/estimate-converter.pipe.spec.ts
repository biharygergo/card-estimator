import { EstimateConverterPipe } from './estimate-converter.pipe';

describe('EstimateConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new EstimateConverterPipe();
    expect(pipe).toBeTruthy();
  });
});

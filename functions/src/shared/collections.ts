/** Used to store short-lived session data for
 * token exchange between Zoom and Google
*/
export const AUTH_SESSIONS = "authSessions";

/** Short-lived pairing for browser SSO link → embedded app custom token handoff */
export const SSO_LINK_PAIRINGS = "ssoLinkPairings";
